import { brand } from './brand';
import { product } from './product';
import { siteSettings } from './siteSettings';
import { aboutPage } from './aboutPage';

export const schemaTypes = [brand, product, siteSettings, aboutPage];
